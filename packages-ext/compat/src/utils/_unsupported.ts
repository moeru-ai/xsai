export const raw = (providers: unknown) => providers
export {
  raw as customProvider,
  raw as experimental_customProvider,
  raw as jsonSchema,
  raw as Output,
  raw as valibotSchema,
  raw as zodSchema,
}
