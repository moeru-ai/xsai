import { createPlayer2 } from './create'

/**
 * Create a Player2 Provider
 * @see {@link https://player2.game}
 * @remarks
 * - baseURL - `http://localhost:4315/v1/`
 */
export const player2 = createPlayer2()
